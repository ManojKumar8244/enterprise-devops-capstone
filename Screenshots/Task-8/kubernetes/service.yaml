apiVersion: v1
kind: Service

metadata:
  name: enterprise-service
  namespace: enterprise-devops

spec:
  type: LoadBalancer

  selector:
    app: enterprise-devops

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
