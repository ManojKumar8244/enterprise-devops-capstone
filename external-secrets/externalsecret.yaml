apiVersion: external-secrets.io/v1

kind: ExternalSecret

metadata:

  name: database-secret

  namespace: enterprise-devops

spec:

  refreshInterval: 1h

  secretStoreRef:

    name: aws-secret-store

    kind: SecretStore

  target:

    name: enterprise-devops-secret

    creationPolicy: Owner

  data:

  - secretKey: username

    remoteRef:

      key: enterprise-devops-dev-database3

      property: username

  - secretKey: password

    remoteRef:

      key: enterprise-devops-dev-database3

      property: password
